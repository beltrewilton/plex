import Config

config :plexcore, Plex.Repo,
  username: "drfadul",
  password: "jeje",
  database: "youir",
  hostname: "puta que pario"


  # hostname = Application.get_env(:plexcore, Plex.Repo)[:hostname]
  # iex -S mix run --config ~/apps/plex/custom_config.exs
